apiVersion: 1

providers:
  - name: "logfilter-dashboards"
    orgId: 1
    folder: "LogFilter"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/dashboards
